{
  "name": "synthex-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run check-types && bun run test && bun run --filter '@synthex/core' build && bun run --filter '@synthex/ui' build && bun run --filter '@synthex/react-web' build && bun run --filter '@synthex/cli' build && bun run --filter '@synthex/web-preview' build",
    "check": "bun run check-types",
    "check-types": "bun run --filter '@synthex/core' typecheck && bun run --filter '@synthex/ui' typecheck && bun run --filter '@synthex/react-web' typecheck && bun run --filter '@synthex/cli' typecheck && bun run --filter '@synthex/web-preview' typecheck",
    "dev": "bun run --filter '@synthex/web-preview' dev",
    "test": "bun run --filter '@synthex/ui' test && bun run --filter '@synthex/react-web' test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "react-router-dom": "^7.9.3",
    "typescript": "^5.9.3"
  }
}
